<%= form_with(model: [local_assigns[:employee],employee_file]) do |form| %>
  <% if employee_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employee_file.errors.count, "error") %> prohibited this employee_file from being saved:</h2>
      <ul>
        <% employee_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :file, style: "display: block" %>
    <%= form.file_field :file %>
  </div>
  <div>
    <%= form.label :document_type, style: "display: block" %>
    <%= form.collection_radio_buttons :document_id, employee.organization.documents, :id, :name   %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
