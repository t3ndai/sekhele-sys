<%= form_with(model: [local_assigns[:organization],asset_category]) do |form| %>
  <% if asset_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(asset_category.errors.count, "error") %> prohibited this asset_category from being saved:</h2>
      <ul>
        <% asset_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
