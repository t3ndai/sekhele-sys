<%= form_with(model: [organization, pulse_survey]) do |form| %>
  <% if pulse_survey.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pulse_survey.errors.count, "error") %> prohibited this pulse_survey from being saved:</h2>
      <ul>
        <% pulse_survey.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :date_open, style: "display: block" %>
    <%= form.date_field :date_open %>
  </div>
  <div>
    <%= form.label :date_close, style: "display: block" %>
    <%= form.date_field :date_close %>
  </div>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
