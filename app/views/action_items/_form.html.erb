<%= form_with(model: [one_to_one, action_item]) do |form| %>
  <% if action_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(action_item.errors.count, "error") %> prohibited this action_item from being saved:</h2>
      <ul>
        <% action_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :completed, style: "display: block" %>
    <%= form.checkbox :completed %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
