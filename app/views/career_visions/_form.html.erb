<%= form_with(model: [personal_development_plan, career_vision]) do |form| %>
  <% if career_vision.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(career_vision.errors.count, "error") %> prohibited this career_vision from being saved:</h2>
      <ul>
        <% career_vision.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
