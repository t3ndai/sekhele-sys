<%= form_with(model: personal_development_plan) do |form| %>
  <% if personal_development_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(personal_development_plan.errors.count, "error") %> prohibited this personal_development_plan from being saved:</h2>

      <ul>
        <% personal_development_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :employee_id, style: "display: block" %>
    <%= form.text_field :employee_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
