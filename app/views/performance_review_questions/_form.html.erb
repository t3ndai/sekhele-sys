<%= form_with(model: performance_review_question) do |form| %>
  <% if performance_review_question.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(performance_review_question.errors.count, "error") %> prohibited this performance_review_question from being saved:</h2>

      <ul>
        <% performance_review_question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :performance_review_type_id, style: "display: block" %>
    <%= form.text_field :performance_review_type_id %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
