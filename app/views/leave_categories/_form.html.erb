<%= form_with(model: [organization, leave_category]) do |form| %>
  <% if leave_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leave_category.errors.count, "error") %> prohibited this leave_category from being saved:</h2>
      <ul>
        <% leave_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
