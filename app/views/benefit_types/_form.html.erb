<%= form_with(model: [:organization, benefit_type]) do |form| %>
  <% if benefit_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(benefit_type.errors.count, "error") %> prohibited this benefit_type from being saved:</h2>
      <ul>
        <% benefit_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
