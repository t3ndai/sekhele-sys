<%= form_with(model: [interview, interviewer]) do |form| %>
  <% if interviewer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(interviewer.errors.count, "error") %> prohibited this interviewer from being saved:</h2>
      <ul>
        <% interviewer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :employee_id, style: "display: block" %>
    <%= form.select :employee_id, @interview.interview_stage.organization.employees.map { |employee| [employee.full_name, employee.id]} %>
  </div>
  <div>
    <%= form.submit "Assign Interviewer" %>
  </div>
<% end %>
