<%= form_with(model: [local_assigns[:organization], company_file]) do |form| %>
  <% if company_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(company_file.errors.count, "error") %> prohibited this company_file from being saved:</h2>
      <ul>
        <% company_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :document_id, style: "display: block" %>
    <%= form.collection_radio_buttons :document_id, organization.documents.company_docs, :id, :name %>
  </div>
  <div>
    <%= form.label :file, style: "display: block" %>
    <%= form.file_field :file %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
